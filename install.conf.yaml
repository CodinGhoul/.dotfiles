- defaults:
    link:
        relink: true
        create: true

- link:
    ~/.dotfiles: ''
    ~/.vimrc:
    ~/.zshrc:
    ~/.ideavimrc:
    ~/.config/nvim/init.vim: neovim/init.vim
    ~/.config/regolith2/Xresources:
        path: regolith2/Xresources
        if: '[ $XDG_SESSION_DESKTOP = regolith ]'
    ~/.gitconfig:
